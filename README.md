# Telegram Code Sender Plugin для PyCharm

## 📋 Описание плагина
Плагин сделал - Караханов Максим, М3103, ИСУ501534

**Telegram Code Sender** - это плагин для PyCharm, который позволяет быстро отправлять выделенный код себе в Telegram чат от имени бота с красивым форматированием.

### 🚀 Основные возможности:
- **Отправка кода** - выделите любой код в редакторе и отправьте его в Telegram
- **Красивое форматирование** - код отправляется в блоке с подсветкой синтаксиса
- **Быстрый доступ** - доступ через правую кнопку мыши или горячие клавиши
- **Визуальное подтверждение** - уведомления об успешной отправке или ошибках

### 🎯 Как использовать:
1. **Выделите код** в редакторе PyCharm
2. **Вызовите плагин** одним из способов:
    - Щелкните правой кнопкой мыши → "Send Code to Telegram"
    - Используйте горячие клавиши: `Ctrl + K` (Windows/Linux) или `Cmd + K` (Mac)
3. **Получите уведомление** об успешной отправке

---

## 🤖 Настройка Telegram бота

### Шаг 1: Создание бота
1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Введите имя вашего бота (например: `MyCodeSenderBot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `MyCodeSenderBot`)
5. **Сохраните токен** который выдаст BotFather - это ваш `BOT_TOKEN`

### Шаг 2: Получение Chat ID

#### Способ 1: Через бота @userinfobot
1. Найдите бота **@userinfobot** в Telegram
2. Начните с ним диалог, отправив любое сообщение
3. Бот ответит с вашим Chat ID - это ваш `CHAT_ID`

#### Способ 2: Через вашего бота
1. Напишите любое сообщение вашему новому боту
2. Откройте браузер и перейдите по ссылке: https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   Замените `<YOUR_BOT_TOKEN>` на токен из шага 1
3. В ответе JSON найдите `"id"` в объекте `"chat"` - это ваш `CHAT_ID`

---

## ⚙️ Настройка плагина

### Обновление конфигурации:
В классе `SendToTelegramAction` замените константы на ваши данные:

```kotlin
companion object {  
    private const val BOT_TOKEN = "ВАШ_ТОКЕН_БОТА"  // Например: "1234567890:ABCdefGHIjklMnOpQRstUVwxyz" 
    private const val CHAT_ID = "ВАШ_CHAT_ID"      // Например: "1759734963"
}
```
## Описание функций

### Класс `SendToTelegramAction`

Основной класс действия, расширяющий `AnAction` из IntelliJ Platform.

### `actionPerformed(event: AnActionEvent)`

Выполняется при активации действия в IDE. Получает выделенный текст из редактора и отправляет его в Telegram.

Параметры:
```
    event (AnActionEvent) - событие, содержащее контекст выполнения действия
```
Логика работы:
```
    Получает текущий проект и редактор из события
    Проверяет наличие активного редактора
    Проверяет наличие выделенного текста
    Форматирует текст для Telegram (оборачивает в code blocks)
    Отправляет текст через Telegram API
    Показывает сообщение о результате операции
```
Ничего не возвращает.
Пример вызова:
```kotlin
val action = SendToTelegramAction() 
action.actionPerformed(event)
```

### `update(event: AnActionEvent)`

Обновляет состояние действия (видимость и доступность) в зависимости от наличия выделенного текста в редакторе.

Параметры:

    event (AnActionEvent) - событие, содержащее контекст для обновления представления
    
Логика работы:

    Делает действие видимым и доступным только когда есть активный редактор и выделенный текст

Ничего не возвращает.
Пример вызова:
```kotlin
val action = SendToTelegramAction()
action.update(event)
```
### `sendToTelegram(text: String)`

Отправляет форматированный текст в Telegram чат через Bot API с использованием MarkdownV2 разметки.

Параметры:

    text (String) - текст для отправки в Telegram (уже форматированный с кодом)

Логика работы:

    Формирует URL для Telegram API
    Устанавливает HTTP соединение с параметрами:
        Метод: POST
        Content-Type: application/json
        Timeout: 10 секунд
    Экранирует специальные символы для JSON
    Формирует JSON тело запроса
    Отправляет запрос и проверяет ответ
Исключения:

    RuntimeException - если Telegram API возвращает код ответа отличный от 200
Ничего не возвращает.
Пример вызова:
```kotlin
sendToTelegram("```\\nprintln(\\\"Hello World\\\")\\n```")
```
